{
  "name": "dcte-kp-teachers-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Admin/one-off Node scripts (Firestore seeding) — never bundled into the Flutter app or Cloud Functions deploy.",
  "scripts": {
    "seed": "ts-node seed/import_seed.ts"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "@types/node": "^20.16.11"
  }
}
